*test
 テストを充実させたい
 構文を増やすごとに構文チェックと
 エラーが正しくでるかのテスト
-そのために、構文パース結果表示を固定したい
-（もとのCを表示するのではなくて、NODEがわかる表示）
-　そのために、デバッグ表示の仕組みをかえたい
-（ちょっとデバッグ表示するとすぐに結果が変わるのをかえたい）
- 今はエラーレベルだけで表示しているが、
- どのデバッグ情報を表示するか指定するようにしよう。

*syntax
 -function parameter
 declarator_list on external declaration
 initializer
 array
 char,short,unsigned
 float,double
 switch,case
 goto,label
 assignment operator
 conditional expr
 operator
 inc,dec
 sizeof
 cast
 enum
 struct,union
 . ->
 ...
 type_qualifier

*code gen
スタックマシンは流石に読み辛かった。
ので、簡単にレジスタを使うことにしよう。
